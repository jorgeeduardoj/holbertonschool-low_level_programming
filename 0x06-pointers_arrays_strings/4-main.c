#include "holberton.h"
#include <stdio.h>

/**
 * string_toupper -  changes all lowercase letters of a string to uppercas
 *
 * Return: char*.
 */
char *string_toupper(char *a)
{
    int i;

    i = 0;
    while (i < n)
    {
        if (i != 0)
        {
            printf(", ");
        }
        printf("%d", a[i]);
        i++;
    }
    printf("\n");
}
